#include <iostream>
#include <vector>
using namespace std;

class Sulation{
public:
    vector<int> sort(vector<int> a, vector<int> b)
    {
        
    }
};

int main(void)
{

    Sulation s;

    vector<int> a;
    vector<int> b;

    //cin >> a >> b;

    vector<int> r = s.sort(a, b);

    return 0;
}